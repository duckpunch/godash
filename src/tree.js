// how will things get used...

var sgf = loadSgf(url);

// making a reviewable tree requires being able to easily traverse and address nodes of a tree

n <-> n
  <-> n


Map {
    next: Map {
        position:
        move_to_get_here:
    }
    position:
    move_to_get_here:
}
