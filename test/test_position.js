import assert from 'assert';
import {sgfToXY} from '../src/position';


//describe('sgfToXY', function() {
    //it('converts "aa" to (0, 0)', function() {
        //assert.deepEqual(sgfToXY('aa'), [0, 0]);
    //});

    //it('converts "hi" to (7, 8)', function() {
        //assert.deepEqual(sgfToXY('hi'), [7, 8]);
    //});

    //it('raises if passed a non-string', function() {
        //assert.throws(function() {
            //sgfToXY(5);
        //}, TypeError);
    //});

    //it('raises if passed string not length 2', function() {
        //assert.throws(function() {
            //sgfToXY('roar');
        //}, TypeError);
    //});
//});
